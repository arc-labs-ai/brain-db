# Rust
/target
**/*.rs.bk
Cargo.lock.bak
*.pdb

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Claude Code config and project context (kept out of version control)
.claude/
CLAUDE.md

# Brain runtime data
/data/
/snapshots/
*.wal
*.arena
*.redb

# Polyglot SDK scratch (Python / TypeScript). Real homes are
# per-language repos under arc-labs-ai; the local /clients/ tree
# is for working-set experiments only.
/clients/

# Benchmarks
/criterion/
/perf.data
/perf.data.old
/flamegraph.svg

# Profiling
/profile.pb

# Logs
*.log
/logs/

# Fuzz
/fuzz/target/
/fuzz/corpus/
/fuzz/artifacts/

# Build artifacts
*.tar.gz
*.zip

# Editor temp files
.envrc

# Eval overlay secret (LLM provider key) — never commit
config/.env.eval

# Deployment secrets (LLM key, admin token) — never commit. `.env.example`
# is the committed template; `.env` is the operator's real values.
.env
