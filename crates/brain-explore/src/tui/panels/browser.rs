//! Left panel: a scrollable list of entities and statements from the
//! current view, with type icons, short ids, and inline confidence.
//! The follow-up plan implements selection, filter input, and the
//! search box; this stub reserves the type name.

#![allow(dead_code)]

/// Left-pane widget. The follow-up plan replaces the unit body with a
/// stateful list backed by `ratatui::widgets::List`.
#[derive(Debug, Default)]
pub struct BrowserPanel;
