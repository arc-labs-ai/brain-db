//! Panel modules for the three-pane explorer layout. Each submodule
//! is a stub today; the follow-up plan fills them in with real
//! ratatui `Widget` implementations and panel-local state.

#![allow(dead_code)]

pub mod browser;
pub mod detail;
pub mod neighborhood;
