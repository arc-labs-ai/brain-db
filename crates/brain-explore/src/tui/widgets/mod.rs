//! Reusable TUI widgets that aren't tied to one panel — the
//! expandable jless-style tree node and the help-overlay cheatsheet.
//! Stubs today; populated in the follow-up plan.

#![allow(dead_code)]

pub mod expandable;
pub mod help_overlay;
